//! Stub placeholder so the Cargo manifest's `[[bin]] alignment-fit` entry
//! resolves in this worktree. The real implementation is being authored on
//! main by a parallel agent; merging that branch will replace this file.

fn main() {
    eprintln!("alignment-fit stub — see main branch for the real binary");
    std::process::exit(2);
}
