name: crates.io Release

on:
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish to crates.io (true/false)'
        required: true
        default: 'false'

permissions:
  contents: read

jobs:
  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    if: github.event.inputs.publish == 'true'

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Refuse prerelease crate publish
        run: |
          VERSION=$(grep '^version = ' Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          echo "Cargo package version: $VERSION"
          if [[ "$VERSION" == *-* ]]; then
            echo "::error::Refusing crates.io publish for prerelease version $VERSION"
            exit 1
          fi

      - uses: Swatinem/rust-cache@v2

      - name: Install LLVM (for RocksDB/clang-sys)
        run: |
          sudo apt-get update
          sudo apt-get install -y llvm-14 clang-14 libclang-14-dev
          echo "LIBCLANG_PATH=/usr/lib/llvm-14/lib" >> $GITHUB_ENV

      - name: Verify package (dry-run)
        run: cargo publish --dry-run

      - name: Publish to crates.io
        run: cargo publish --token ${{ secrets.CARGO_REGISTRY_TOKEN }}
